function encodePwd(pwd: string): string {
  return pwd;
}

function decodePwd(pwd: string): string {
  return pwd;
}

function checkPwd(pwd: string, userPwd: string): boolean {
  return pwd === userPwd;
}

export { encodePwd, decodePwd, checkPwd };
